import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nieva.relevamientovisual',
  appName: 'Relevamiento Visual',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
